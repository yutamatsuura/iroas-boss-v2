import React from 'react';
import { Box, Typography } from '@mui/material';

/**
 * P-005: 報酬計算
 * 7種類のボーナス計算実行と計算結果確認
 */
const Rewards: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">報酬計算</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        報酬計算ページ - 実装中（Step 17で完全実装）
      </Typography>
    </Box>
  );
};

export default Rewards;